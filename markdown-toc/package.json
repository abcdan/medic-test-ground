{
  "name": "markdown-toc",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "bin": { "mdtoc": "dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test dist/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.3"
  }
}

{
  "name": "medic-test-ground",
  "version": "1.0.0",
  "private": true,
  "description": "A tiny Express app used to exercise an AI code reviewer.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
